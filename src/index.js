import React from 'react'
import ReactDOM from 'react-dom'
import Home from './AdaptComponent/Home'

ReactDOM.render(
  <React.StrictMode>
    <Home />
  </React.StrictMode>,
  document.getElementById('root'),
)
